'use strict';

/**
 * @ngdoc function
 * @name angGraderApp.controller:ProfileCtrl
 * @description
 * # ProfileCtrl
 * Controller of the angGraderApp
 */
angular.module('angGraderApp')
  .controller('ProfileCtrl', function ($scope, $rootScope, Assignment, Students) {
console.log('inside ProfileCtrl');
  });
